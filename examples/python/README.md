# Python

```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
export GB_TOKEN=...   # mint at https://app.gitbrain.com/dashboard — never the prompt
python hello_gitbrain.py
```
